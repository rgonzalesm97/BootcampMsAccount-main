spring:
  application:
    name: account
#  config:
#    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888/}
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      username: nttdata
      password: nttd4t4